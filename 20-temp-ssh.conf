# Temporary SSH users get password auth via group membership.
# Root and normal users unaffected (still key-only).
Match Group tempssh
    PasswordAuthentication yes
    KbdInteractiveAuthentication yes
